/** Primary site logo — matches the live TheAgileNest site (`public/1.png`). */
export const DEFAULT_LOGO_SRC = "/1.png";

/** PMI Authorized Training Partner badge shown in the footer. */
export const DEFAULT_PMI_BADGE_SRC = "/2.png";

/** Site favicon / compact mark. */
export const DEFAULT_FAVICON_SRC = "/favicon.png";
